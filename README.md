# 📊 CSV Analyzer

> Subí cualquier archivo CSV y obtené análisis automático con estadísticas, visualizaciones y procesamiento de texto.

## 🚀 Correr localmente

```bash
# Setup (solo la primera vez)
chmod +x scripts/*.sh
./scripts/setup.sh

# Correr
./scripts/run.sh

# Abrir http://localhost:8050
